import { Check } from 'lucide-react'

const options = [{ value: 'light', label: 'Light', description: 'Warm cream surfaces' }, { value: 'dark', label: 'Dark', description: 'Ink courtyard' }, { value: 'system', label: 'System', description: 'Follow your device' }]

export default function ThemeSelector({ value, onChange }) { return <div className="grid gap-2 sm:grid-cols-3">{options.map((option) => <button key={option.value} type="button" onClick={() => onChange(option.value)} className={`flex min-h-20 items-start gap-3 rounded-control border p-3 text-left transition ${value === option.value ? 'border-[var(--dh-primary)] bg-[color-mix(in_srgb,var(--dh-primary)_9%,transparent)]' : 'border-[var(--dh-border)] hover:border-[var(--dh-primary-soft)]'}`} aria-pressed={value === option.value}><span className={`mt-0.5 grid h-5 w-5 place-items-center rounded-full border ${value === option.value ? 'border-[var(--dh-primary)] bg-[var(--dh-primary)] text-white' : 'border-[var(--dh-border)]'}`}>{value === option.value && <Check size={13} />}</span><span><span className="block text-sm font-semibold">{option.label}</span><span className="mt-1 block text-xs leading-5 text-[var(--dh-muted)]">{option.description}</span></span></button>)}</div> }
